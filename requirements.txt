pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0  # 환경변수 관리

# PostgreSQL 연결
psycopg2-binary>=2.9.0  # PostgreSQL 드라이버
sqlalchemy>=2.0.0       # ORM / 연결 관리

# 시각화 관련 (Stage 2)
matplotlib>=3.5.0  # 히트맵 렌더링
seaborn>=0.12.0  # 색상 스케일
plotly>=5.0.0  # 인터랙티브 차트 (Option 2)
optuna>=3.0.0  # Bayesian Optimization (Walk-Forward)
streamlit>=1.30.0  # 웹 대시보드 (Stage 5)
